
import * as ActionTypes from "./ActionTypes";



export const InitialFeedback=(state={
    feedbackInfo:[],
    },
    action     )=>{
    switch (action.type){
    
        default:
            return state
    }
}

